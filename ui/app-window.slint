import {MyButton} from "./MyButton.slint";
import {Grid_Keys} from "./grid_keys.slint";

export component MainWindow inherits Window {
    width: 350px;
    height: 600px;
    background: #cfc3c3;

    Grid_Keys {}
}
